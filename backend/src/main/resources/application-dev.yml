server:
  port: 8080

spring:
  application:
    name: lib-seat-back
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://pub-test.milolab.cn:32768/seat?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: seat
    password: alipapa

mybatis:
  type-aliases-package: cn.alipapa.seat.bean
  mapper-locations:
    - classpath:mapper/*Dao.xml

logging:
  level:
    cn:
      alipapa:
        seat:
          dao: debug
