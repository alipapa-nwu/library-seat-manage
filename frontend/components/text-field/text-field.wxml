<!--components/text-field/text-field.wxml-->
<view class="text-field">
  <text class="label block non-shrink">{{label}}</text>
  <input
  class="block grow"
  placeholder="{{placeholder}}"
  model:value="{{value}}"
  type="{{type}}"
  password="{{password}}"></input>
</view>