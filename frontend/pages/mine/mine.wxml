<!--pages/mine/mine.wxml-->
<view>
	<self-information name="{{name}}" stu_num="{{stu_num}}" />
	<status-button wx:for="{{reservations}}" wx:key="index" status="{{item.status}}" description="{{item.description}}"/>
</view>