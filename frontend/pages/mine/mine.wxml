<!--pages/mine/mine.wxml-->
<view>
	<self-information name="{{name}}" stu_num="{{stu_num}}" />
	<status-button
		wx:for="{{reservations}}"
		wx:key="index"
		status="{{item.status}}"
		description="{{item.description}}"
		bind:tap="onTap"
		data-description="{{item.description}}"
		data-status="{{item.status}}"/>
</view>